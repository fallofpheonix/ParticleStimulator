# Frontend Mirror

This directory mirrors the browser assets served from `src/web/static/`.

- `simulation_dashboard/`: runnable static bundle copy for repository visibility
- `event_viewer/`: reserved for future event-display-specific UI modules
- `ui_controls/`: reserved for future standalone control widgets

Runtime source of truth remains `src/web/static/`.
